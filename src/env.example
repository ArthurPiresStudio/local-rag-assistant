# ── Supabase ─────────────────────────────────────────────────────────
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_KEY=your-supabase-anon-key        # usado pelo FastAPI local
SUPABASE_ANON_KEY=your-supabase-anon-key   # alias usado no rag.py

# ── Ollama (local) ───────────────────────────────────────────────────
# Não precisa de chave — só garantir que o Ollama está rodando em localhost:11434
# Modelos necessários:
#   ollama pull deepseek-coder
#   ollama pull nomic-embed-text  (só se for indexar via Python local)
